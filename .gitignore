# ESP-IDF build output
build/

# Per-project configuration (generated by `idf.py menuconfig` / build)
sdkconfig
sdkconfig.old

# Managed components installed by the component manager
# (idf_component.yml stays tracked; the fetched sources don't)
managed_components/
dependencies.lock

# Edge Impulse SDK export (regenerado a partir do projeto no Edge Impulse Studio)
components/edge-impulse-sdk/
components/model-parameters/
components/tflite-model/

# Editor / IDE
.vscode/
.idea/

# OS cruft
.DS_Store