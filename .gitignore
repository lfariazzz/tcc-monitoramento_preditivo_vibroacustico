# ESP-IDF build output
build/

# Per-project configuration (generated by `idf.py menuconfig` / build)
sdkconfig
sdkconfig.old

# Managed components installed by the component manager
# (idf_component.yml stays tracked; the fetched sources don't)
# dependencies.lock, por outro lado, FICA versionado — é o que garante
# que quem clonar resolva exatamente as mesmas versões (ver README, Seção 5.2)
managed_components/

# Editor / IDE
.vscode/
.idea/

# OS cruft
.DS_Store

.vscode